# ============================================================================
# 备胎忽略规则：当前构建上下文是仓库根目录，实际生效的是根目录 .dockerignore。
# 若将来把 backend 的 build context 改为 ./backend，此文件才会被 Docker 读取。
# ============================================================================
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
tests/
scripts/
app/data/app.db
